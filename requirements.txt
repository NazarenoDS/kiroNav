# KiroNav - Dependencies
flet>=0.86.0
mss>=9.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
openai>=1.0.0

# AI inference runs through Kiro Gateway (local HTTP API).
# See README.md for setup instructions.

# Wayland sessions also need `grim` from the system package manager.
#   Arch:   sudo pacman -S grim
#   Debian: sudo apt install grim

# Dev Dependencies
# pytest>=7.0.0
# ruff>=0.1.0
